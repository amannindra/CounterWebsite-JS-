console.log("Check");
let count = 0;

const value = document.querySelector("#Value");
const btns = document.querySelectorAll(".Gen");

console.log(btns);

btns.forEach(function (Gen) {
  Gen.addEventListener("click",function(e){
    
  })
});
